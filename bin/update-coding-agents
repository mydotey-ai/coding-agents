#!/bin/bash

set -x

source ~/bin/enable-proxy

claude update
claude plugin marketplace update

opencode upgrade

uv tool upgrade specify-cli

npm upgrade -g happy
npm upgrade -g @twsxtd/hapi
npm upgrade -g gitnexus

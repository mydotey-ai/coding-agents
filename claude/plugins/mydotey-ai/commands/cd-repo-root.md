# cd-repo-root

切换到当前 git 仓库的根目录。

## 执行步骤

1. **切换到项目根目录**
   - 运行 `git rev-parse --show-toplevel` 获取 git 根目录路径
   - 如果当前目录不是根目录，切换到根目录
   - 如果已经是根目录，提示用户无需切换

## 错误处理

- 当前目录不在 git 仓库中：提示用户先进入一个 git 仓库
